# 🌦️ Weather App

A simple weather application built using **HTML**, **CSS**, and **JavaScript**.  
Users can enter a location, fetch weather data from an external API, and view the processed results directly on the page.

---

## 🚀 Features

- Fetch real-time weather data based on user input
- Separate modules for:
  - API fetching
  - Data processing
  - DOM updates
- Optional loading indicator
- Optional dynamic icon loading using `import()`
- Fully customizable styling

---
